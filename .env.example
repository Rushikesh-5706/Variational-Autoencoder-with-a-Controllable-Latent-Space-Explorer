# Streamlit server port — also used by docker-compose port mapping and healthcheck
STREAMLIT_SERVER_PORT=8501

# Latent space dimensionality — must match the checkpoint used in models/vae.pt
# If you retrain with a different value, update this and rebuild the Docker image
LATENT_DIM=16
